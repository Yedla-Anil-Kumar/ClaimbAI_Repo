Example payload (lineage practices readiness):

{
  "metric_id": "lineage_practices_readiness_band",
  "evidence": {
    "git_sha_logging": ["mlflow.set_tag('mlflow.source.git.commit', GIT_SHA)"],
    "data_refs": ["s3://bucket/dataset/v3"],
    "env_locks": ["conda.yaml","requirements.txt"]
  },
  "rubric": {
    "5": "git_sha + data_ref + env lock all logged",
    "4": "two present",
    "3": "one present",
    "2": "weak hints",
    "1": "none"
  }
}